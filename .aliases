# Useful keys

alias wget='wget -c'
alias mkdir='mkdir -pv'
alias less='less -FX'

# Short names

alias lg="lazygit"
alias tf="terraform"
alias be="bundle exec"

# Virtual environment for python

alias mkvenv='python3.12 -m venv ./venv && source ./venv/bin/activate.fish && pip install --upgrade pip'
alias activate='source ./venv/bin/activate.fish'

# Brew M1
alias brew='/opt/homebrew/bin/brew'

alias k='kubecolor'
alias kubectl='kubecolor'
alias kx='kubectx'
alias kns='kubens'
alias klogs='stern --tail 20'

alias reload='source ~/.zshrc'
alias zshconfig='vim $HOME/.zshrc'

alias ls='exa'
alias cat='bat -p'

alias wfvpn='sshuttle -r ansible@54.197.160.159 10.0.0.0/8'
